# OpenRouter API Configuration
# Get your API key at https://openrouter.ai

# Option 1: Configure backend API key (recommended for security)
# This keeps the API key on the server side
NUXT_OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxx

# Option 2: Let users provide their own API keys
# Users will be able to input their keys in the UI for privacy
# No server-side key needed

# Site URL for OpenRouter referrer header
NUXT_PUBLIC_SITE_URL=http://localhost:3000
